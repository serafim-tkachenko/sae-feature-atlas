[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "sae-feature-atlas"
version = "0.4.0"
description = "Research toolkit for building SAE feature atlases over Gemma Scope models."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.10,<3.13"
authors = [
    { name = "Serafim Tkachenko" }
]

dependencies = [
    "torch",
    "transformer-lens",
    "sae-lens",
    "transformers",
    "datasets",
    "accelerate",
    "einops",
    "pandas",
    "pyarrow",
    "numpy",
    "scikit-learn",
    "umap-learn",
    "scipy",
    "tqdm",
    "matplotlib",
    "huggingface-hub",
]

[project.urls]
Repository = "https://github.com/serafim-tkachenko/sae-feature-atlas"
Documentation = "https://github.com/serafim-tkachenko/sae-feature-atlas/tree/main/docs"

[dependency-groups]
dev = [
    "ipykernel",
    "jupyter",
    "pytest",
    "ruff",
]

[project.scripts]
sae-atlas = "sae_feature_atlas.app:main"

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"
